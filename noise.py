import numpy as np

class Noise(object):
    def __init__(self, bool_arr):
        self.size = len(bool_arr)
        self._bool_arr = bool_arr
        self.sample_arr = [0, 1]
        self.noise = np.random.choice(self.sample_arr, self.size)

        if np ran [ 0 , 1] >0,5 zaszum


    def addingNoise(self):
        for i in range(self.size)
            if()